# RotPitch — Tasks

## Phase 1 — Foundation, DB, shared, design system, auth

- [x] Scaffold Turborepo (pnpm): apps/web, apps/api, packages/db, packages/shared; turbo.json, tsconfig base, eslint/prettier, .env.example
- [x] packages/db: SQL migrations (4 tables, enums, FKs, indexes, RLS own-rows), signup trigger (1 free credit), Supabase type-gen script
- [x] packages/shared: PLANS constant (credits, gating, price) + zod schemas
- [x] Design system: seed tailwind.config.ts + CSS variable tokens from PRD spec, three theme variants, fonts (Clash Display, Satoshi, JetBrains Mono)
- [x] Auth: Supabase email/password + Google OAuth (@supabase/ssr), browser/server clients, session-refresh middleware, protected route group, server guard, signup awards 1 free credit
- [x] Auth screens: Sign Up + Log In built against the design system
- [x] /theme-check page (Signal button + glow, volt focus-ring input, card, mono credit pill)

### Phase 1 — blocked / pending input
- [x] Auth flow code completed: bumped `@supabase/ssr` 0.5.2 → **0.10.3** (peer `supabase-js ^2.105.3`) — fixes the typed-query `never` regression (verified). Signup now **instant-login** when `data.session` exists (email-confirmation OFF), falls back to "check inbox" if ON. Login surfaces `?error=auth_callback_failed`. Open-redirect hardening via `lib/safe-redirect.ts` (callback + AuthForm). **Password reset** added: `/forgot-password` (`resetPasswordForEmail` → recovery link via `/auth/callback?redirectedFrom=/reset-password`) + `/reset-password` (`updateUser`), "Forgot?" wired. All pages verified HTTP 200.
- [x] **Supabase project provisioned + verified** (project `xbvnzfyheimezuikhpnz`). Env wired (root `.env` + `apps/web/.env.local` synced with real anon key; `SUPABASE_DB_URL` password URL-encoded). Verified live: all 4 tables exist, `users` schema matches migration, **signup trigger grants 1 free credit** (users + credit_transactions rows), **password login** returns a session, **RLS own-row** read works, anon sees no rows, and the web middleware does a real `getUser`/redirect with no errors.
- [x] Signup confirmation switched to **OTP code entry** (not magic link): signup → `/verify-email?email=…` → `VerifyOtpForm` (`verifyOtp` type `signup`, with resend) → session → `/app`. Avoids the magic-link→/app 431. Also bumped dev `--max-http-header-size=65536` as a stale-cookie safety net.
- [~] **Owner: remaining Supabase dashboard config**:
  1. [x] **Confirm signup** email template wired to `{{ .Token }}` (6-digit OTP). All 6 auth email templates branded to the design system in `supabase/email-templates/` (confirm-signup, magic-link, reset-password, change-email, invite, reauthentication).
  2. [x] **Google OAuth enabled, tested, working** (Auth → Providers → Google + Google Cloud OAuth client).
  3. [~] Auth → **URL Configuration** set. **SMTP deferred** (owner: future) — reset/confirmation emails won't send via custom SMTP until configured; default Supabase mailer applies.
  4. [ ] (Optional) `supabase login` → `pnpm db:gen-types` to replace the hand-written db-types stub with generated types — current stub already matches the verified live schema.
  5. [ ] Service-role key belongs in `apps/api/.env` when the API is built (Phase 4) — never in the web app.
- [x] Second brand mark — clapperboard + price-tag (video × pitch), recreated as SVG from the supplied screenshot (mask-punched cuts so stripes/moat/hole read as background on any surface). `Brand` is now **switchable** via one constant `DEFAULT_BRAND_VARIANT` in `lib/brand.ts` (`'clapper'` default | `'layers'`), with per-usage `variant` override. Standalone asset at `public/svg/rotpitch-logo-clapper.svg`. Favicons still use the layer-stack pack — regenerate from the clapper if it should become the browser/PWA icon too.
- [x] Brand logo integrated from the official asset pack (`rotpitch-logo/`, "Concept 04 · Layer Stack"). `Brand` component now renders the real mark (violet back panel + volt-lime front panel + ink play glyph, inlined from `svg/rotpitch-logo-primary.svg`; `markOnly` prop available) with the Clash Display wordmark per guidelines — flows to nav/footer/auth/sidebar. Assets copied to `apps/web/public/` (favicon/, app-icons/, social/, svg/, png/, site.webmanifest); root layout wired with `icons` + `manifest` + `openGraph`/`twitter` (og-image) + `viewport.themeColor #CBFF3D`. All assets serve 200.
- [x] Connect **Stitch MCP** (Google) — `projects/6409736880352794320` "RotPitch Landing Page" has the full screen set (landing, auth, dashboard, upload, pricing, billing, modals).
- [x] Marketing landing page (`app/page.tsx`) ported from the Stitch "RotPitch Landing Page" screen onto the project design system: Lucide icons, Syne/DM Sans marketing type (`font-syne`/`font-dm` + `signal-gradient`/`aurora-mesh`/`aurora-text`/`glass-panel`/`nebula-border` utilities), interactive before/after slider + FAQ, **real PLANS data** in pricing (not Stitch placeholder copy). Verified rendering at HTTP 200.
- [x] Next webpack `extensionAlias` fix so `@rotpitch/shared` (`.js` NodeNext specifiers → `.ts`) bundles at runtime.
- [x] **Decision resolved:** migrate the whole app to the **Stitch fonts** (Syne + DM Sans + JetBrains Mono). Repointed the type tokens in `globals.css` (`--font-display`/`--font-syne` → Syne; `--font-ui`/`--font-dm` → DM Sans), dropped the Fontshare (Clash/Satoshi) import. No component edits needed (all reference CSS vars). Verified: typecheck clean, `/`, `/login`, `/theme-check` all 200, compiled CSS loads Google Fonts with 0 Clash/Satoshi refs. CLAUDE.md §10 + Decisions Log updated.
- [x] Swap **auth screens** (login/signup) to the Stitch markup — centered card over an aurora field (`AuroraField` cursor parallax, reduced-motion safe), mono labels, password eye-toggle, Stitch footers (signup tagline / login system-status). Supabase email+password+Google OAuth wiring preserved. Verified both at HTTP 200.
- [x] Dashboard / Video Library ported from Stitch — protected app **shell** (`(protected)/layout.tsx`): fixed `SideNav` (brand, active-route nav via usePathname, credit widget, sign-out) + `AppHeader` (search, New Video, notifications, avatar initial). Library page: title + filter pills + responsive `VideoCard` grid (done/processing/failed + batch badge) + add-new card + `EmptyState` (Stitch empty screen). Presentational `DEMO_VIDEOS` placeholder until the `videos` query is wired. Removed the profile DB read from the page → **whole web app now typechecks clean** (`never` error gone). Verified at HTTP 200 via temporary guard bypass (restored).
- [x] Wire real data into the shell/library: server data layer `lib/data.ts` (`getProfile`, `getVideos`, `getCreditTransactions`) + display mappers `lib/format.ts` (`relativeTime`, `humanizeStyle`, `videoRowToCard`). Protected `layout.tsx` now reads the real `users` profile (plan + credits → `SideNav`, email → `AppHeader`); library page renders the real `videos` query (empty-state branch intact). `VideoCard` `duration`/`progress` made optional (not persisted) with indeterminate processing bar. Re-enabled typed selects (safe on `@supabase/ssr@0.10.3` + `supabase-js@2.106.2`). Typecheck + prod build clean; protected routes 307→/login, public 200.
- [x] Build the nav destination screens: **`/app/credits`** (real balance + ledger from `credit_transactions`), **`/app/billing`** (current plan + 4-plan comparison from `PLANS`; upgrade/downgrade CTAs disabled pending Phase 7 checkout), **`/app/settings`** (account + `ThemeSwitcher` (3 CSS-var themes, persisted) + sign-out + danger-zone placeholder), **`/app/create`** (`CreateForm` client: upload dropzone, plan-gated background picker w/ placeholder catalog `lib/backgrounds.ts`, format/captions/voiceover/auto-generate toggles all gated per `PLANS`, credit-cost calc + validation; submit surfaces "render pipeline lands in Phase 5").
- [~] Reconcile remaining app screens with Stitch. **Decision (owner):** keep the condensed `/app/create` (Upload + Pick Background + Generate + Auto Generate in one config surface) — NOT splitting into the separate Stitch wizard steps. Ported the genuinely-missing pieces:
  - [x] **3 modals** (`components/ui/Modal.tsx` base — scroll-lock, Esc/click-out close, focus restore, reduced-motion-safe `rp-pop-in`/`rp-overlay-in`): **`OutOfCreditsModal`** + **`UpgradeModal`** (plan-driven from `PLANS`, benefits/price/badge derived) wired live into `CreateForm` (locked feature/style → Upgrade; credit shortfall on Generate → Out-of-Credits); **`DeleteVideoModal`** wired into the library via new client **`LibraryGrid`** (hover trash affordance per finished/failed card; confirm shows honest "wires up in Phase 5" toast).
  - [x] **`/app/rendering`** (Rendering Progress) + **`/app/preview`** (Video Preview) — presentational ports with demo data + a blue "Phase 5" note banner; Lucide icons + design-system tokens. Preview becomes the per-video route (`/app/videos/[id]`) when render output exists.
  - Verified: typecheck + lint clean, prod build compiles all 18 routes (incl. the two new pages).
- [ ] Provision the real Supabase project, run migrations, `pnpm db:gen-types` to replace the hand-written db-types stub. (Also fixes the `@supabase/ssr@0.5.2` vs `supabase-js@2.106` typing mismatch that makes `app/(protected)/app/page.tsx` queries resolve to `never` — bump `@supabase/ssr` when revisiting.)

## Phase 4 + 5 — Render pipeline (happy-flow slice DONE, verified end-to-end)
- [x] **creditService** (`apps/api/src/services/creditService.ts`) — `deductCredits` (all-or-nothing batch) + `refundCredit`, via race-safe SQL functions (`packages/db/migrations/0003_credit_functions.sql`: `deduct_credits(uuid, uuid[])` row-locks the user + writes one `use` ledger row per video; `refund_credit`). _Lazy expiry / monthly reset still TODO (Phase 7 billing)._
- [x] **Storage** — 3 Supabase buckets (`backgrounds` public, `raw-uploads` private 50 MB, `outputs` public) + RLS so the browser uploads only under its own uid folder (`0004_storage.sql`). All output on Supabase Storage for now; S3 is the prod target.
- [x] **API routes** (`apps/api/src/routes/videos.ts`) — `POST /generate`, `POST /auto-generate` (batch, distinct styles, charged upfront, rolled back on payment failure), `GET /:id` (status poll). Full server-side gating; never trusts the client.
- [x] **Queue + worker** — BullMQ (`lib/queue.ts`; connection opts so BullMQ uses its bundled ioredis; Pro = high priority) + render worker (`workers/renderWorker.ts`, `worker.ts` entry, `dev:worker`/`start:worker` scripts): download raw + bg → FFmpeg → upload output → mark done; on failure mark failed + refund.
- [x] **FFmpeg composite** (`services/ffmpeg.ts`) — split-screen (9:16 vstack product-top/bg-bottom; 16:9 hstack), bg looped + `-shortest`, free-plan watermark via **overlay of the brand PNG** (`apps/api/assets/watermark.png`) — NOT drawtext, so it works on ffmpeg without libfreetype (incl. local Homebrew ffmpeg).
- [x] **Web wiring** — `lib/api.ts` (browser uploads to `raw-uploads`, then bearer-authed POST), `CreateForm` submits for real (single + auto), lands on library, `AutoRefresh` polls while rendering, `VideoCard` plays/downloads the real `output_url`.
- [x] **Verified end-to-end (free user):** generate → pending→processing→done (~8s), 1 credit deducted + `use` ledger row, public 1080×1920 H.264+AAC output with watermark composited. Web typechecks + prod-builds clean.
- [x] **Delete video** — `DELETE /api/videos/:id` (own rows; drops queued job, deletes row + output object, removes the raw upload only if no Auto-Generate sibling still references it; no credit refund). Web `LibraryGrid` trash now calls it for real (`deleteVideo` in `lib/api.ts`, modal `pending` state) + refreshes. Verified live: row + output + raw removed, 404 for already-gone/unknown id, 401 unauthenticated.
- [x] **Edit phase (zoom + mute)** — new "Edit & preview" step in `CreateForm` (shown once demo + background picked): live split-screen preview (`PreviewComposite`/`PreviewPanel`, CSS `scale` mirrors the FFmpeg crop-zoom) + demo/background zoom sliders (`ZoomSlider`, 1×–2.5×) + mute toggle. Shared `editOptionsSchema` (`productScale`/`backgroundScale`/`muted`) merged into `generateVideoSchema` + `autoGenerateSchema`; carried through `RenderJob` → worker → `renderComposite`. FFmpeg cell scaling now `scale(cell×factor)+centre-crop`; mute drops the `0:a?` map for `-an`. Batch-wide (one setting for all variants). Web + api + shared typecheck clean.
- [x] **Edit phase (zoom + mute) verified working end-to-end** (owner-confirmed). Captions/voiceover still unimplemented in the worker (Phase 6), so those toggles save flags that do nothing today.
- [x] **Library filter pills functional** — `LibraryGrid` owns filter state (All / 9:16 / 16:9 / Status=not-done); were previously decorative.
- [x] **Upload validation (3 layers).** Limits in `packages/shared` (`MAX_UPLOAD_BYTES`=50 MB, `MAX_INPUT_DURATION_SEC`=60, `ACCEPTED_VIDEO_MIME/EXTENSIONS`, `validateUploadMeta`/`validateDuration`). **(A) Client** `CreateForm.onFiles` — type+size then duration via off-DOM `<video>` (`readVideoDuration`); custom-bg upload checks type+size only (loops, no duration cap); demo input `accept` narrowed to the MIME list. **(B) Bucket** `raw-uploads` now carries `allowed_mime_types` + 50 MB cap (0004_storage.sql, on-conflict updates both). **(C) Worker (authoritative)** `renderWorker` runs `probeDurationSec` (ffprobe) after download → rejects undecodable/over-60s with a `RenderError`; failure handler stores a user-facing reason. New `videos.failure_reason` column (0005), surfaced via `GET /:id` + library `VideoCard`. Typecheck clean; migrations applied + verified live (column present, bucket mime/limit set). _Not yet exercised with a real over-limit upload end-to-end._
- [ ] Remaining Phase 5: per-job progress % (**deferred** — ~8 s renders make it cosmetic), S3 output for prod (**deferred to Phase 9** — clean swap at `uploadFrom`/`OUTPUT_BUCKET`). Owner background asset pack ✅ uploaded (copyright-free gameplay loops).

## Phase 6 — captions + voiceover
- [x] **Auto-captions (OpenAI Whisper → libass burn-in).** Worker step (when `hasCaptions`): `probeHasAudio` → `extractAudio` (mono 16 kHz mp3) → `transcribeAudio` (`services/whisper.ts`, native fetch to OpenAI `audio/transcriptions`, `verbose_json` + segment timestamps, no SDK) → `writeAssFile` (`services/captions.ts`, styled bottom-centre ASS: bold white + black outline, libass auto-wrap) → `renderComposite` burns it via the `subtitles` filter. No audio / no speech → renders **without** captions (not a failure); transcription/API error → job fails + credit refunds. Binaries configurable via `FFMPEG_BIN`/`FFPROBE_BIN`; `OPENAI_API_KEY` optional (caption job without it fails with a clear reason). Web already sent `hasCaptions` (single + auto), so no web change. **Verified:** segment→ASS→libass burn renders 1080×1920 H.264+AAC with correctly wrapped captions (via the real `writeAssFile`+`renderComposite` path). _Live Whisper HTTP call untested — needs `OPENAI_API_KEY` in `.env`._
  - **Local infra:** the homebrew-core `ffmpeg` had no libass/freetype → swapped to `homebrew-ffmpeg/ffmpeg/ffmpeg` (8.1.1, libass+freetype+fontconfig+harfbuzz). Railway prod ffmpeg (apt) already has libass. **Follow-up (Phase 9):** bundle a brand font + pass `fontsdir` so prod doesn't depend on system fonts (today libass falls back to Arial/system sans).
- [x] **Caption editor (place / resize / style).** Shared `CAPTION_PRESETS` (5: Classic/Punch/Boxed/Volt/Clean) + `captionOptionsSchema` (`captionStyle`, normalized `captionX`/`captionY` centre, `captionScale`) merged into both render schemas; single source of truth for the web preview (CSS) and worker (ASS). Worker `buildAss` now positions via `\an5\pos(cx,cy)`, sizes via `CAPTION_BASE_FONT × scale`, styles from the preset (colour/outline/bold/uppercase/box → BorderStyle 3), and pre-wraps text with `\N`. Web `CreateForm`: when captions are on, the Edit & preview composite shows a **draggable + corner-resizable** caption placeholder (pointer-capture, normalized pos clamped 0.06–0.94, scale 0.6–2×) over the frame, plus a **Caption style** picker (preset chips + size slider). Settings flow web→API→worker. **Verified:** repositioned + Volt preset + 1.4× renders correctly through the real ASS→libass path (frame inspected). _Hardening: a silent product + `-stream_loop -1` + `-shortest` can infinite-loop ffmpeg — `renderComposite` now takes a `durationSec` cap (`-t`, set to the probed product length in the worker) as a hard backstop. Verified: the silent-product case that hung now exits in ~2s with a correctly-capped output._
- [~] Phase 6 — voiceover (ElevenLabs) — **deferred: surfaced as "Coming soon" pending user feedback** (owner decision). UI: Create form toggle is a non-interactive "Coming soon" tile; billing comparison shows a "Soon" tag; UpgradeModal + landing say "(coming soon)". Server: `POST /generate` + `/auto-generate` reject `hasVoiceover` universally (`Forbidden('AI voiceover is coming soon')`) so it's truly off on every path and never charged. When built: reuse the Whisper transcript as the script (Decisions Log).

## Later phases (not started)
- [ ] Phase 7 — billing (Stripe + Razorpay, idempotent webhooks, lifecycle)
- [ ] Phase 8 — frontend product flows (upload, background picker, generate config, library, credits, billing)
- [x] Phase 9 — hardening + deploy (Vercel + Railway). **DEPLOYED & verified end-to-end (2026-06-09):** upload → enqueue (Upstash) → worker → ffmpeg composite → Supabase `outputs` → download, live in prod. Web on **Vercel** (`rotpitch-web.vercel.app`), API + worker on **Railway** (api `rotpitchapi-production.up.railway.app`; worker connected to Upstash `rediss://`), Redis on **Upstash** (TLS), DB/Auth/Storage on **Supabase**.
  - [x] **Prod runtime fix.** The `tsc` build was non-runnable: emitted JS still imports `@rotpitch/shared` (whose `main` is raw `src/index.ts` → `ERR_UNKNOWN_FILE_EXTENSION` at `node dist/index.js`), and asset paths use `import.meta.url` (bundling would break `../../assets/watermark.png`). Fix: **run prod via `tsx` on source** (mirrors dev). `apps/api` prod scripts `start`/`start:worker` drop `--env-file` (platform injects env); `tsx` moved to `dependencies`. Verified: `pnpm --filter @rotpitch/api start` boots → `[api] listening on :4000` (Redis ECONNREFUSED only because no local Redis — Upstash supplies `REDIS_URL` in prod).
  - [x] **`index.ts` binds `$PORT`** (Railway-injected) with `API_PORT` fallback.
  - [x] **`apps/api/Dockerfile`** — `node:20-bookworm-slim` + `ffmpeg` (libass) + `fontconfig`/`fonts-liberation` (caption fallback font — addresses the Phase 6 `fontsdir` follow-up) + pnpm workspace install + tsx runtime. Build context = repo root. Worker service overrides CMD with `start:worker`.
  - [x] **`.dockerignore`** (root) — excludes node_modules/dist/.next/.turbo/.git and all `.env*` (secrets injected, never baked).
  - [x] **`DEPLOY.md`** — click-by-click for GitHub push → Upstash → Railway (2 services) → Vercel → Supabase auth URLs → cross-wiring → smoke test.
  - [x] **External (owner):** pushed to GitHub; Upstash Redis created; Railway api + worker services; Vercel web; Supabase Auth allow-list updated (Site URL + `…vercel.app/**` redirect); `NEXT_PUBLIC_API_URL` ↔ `WEB_ORIGIN` cross-wired.
  - [x] **Prod fix — Next.js CVEs.** Railway's dependency scanner blocked the API build on two HIGH Next.js CVEs (CVE-2025-55184, CVE-2025-67779) found in the workspace lockfile. Bumped `next`/`eslint-config-next` 14.2.13 → 14.2.35 (patch-level).
  - [x] **Prod fix — render OOM.** First prod render died with `ffmpeg exited null` (SIGKILL) the instant x264 began encoding: in-container ffmpeg saw the host's full core count and x264 pre-allocated per-thread lookahead buffers at 1080×1920 → multi-GB → kernel OOM-kill. Fixed by capping threads via new `FFMPEG_THREADS` env (default 2), applied to `-threads` (decode+encode) + `-filter_complex_threads` in `services/ffmpeg.ts`. Render verified after the fix.
  - **Deferred (per scope):** S3 output (stays Supabase), brand caption font + `fontsdir`, billing (Phase 7 inert at launch — checkout non-functional), voiceover.
- [~] Phase 10 — **migrate backend to AWS** (web stays on Vercel; auth/DB + `raw-uploads`/`backgrounds` stay on Supabase). **Code-complete (2026-06-09); pending EC2 provisioning + live verify.**
  - [x] **Output → AWS S3** (private, presigned GET). New `apps/api/src/services/s3.ts` (`uploadOutput`/`presignOutput`/`deleteOutput`) + `apps/web/lib/s3.ts` (`presignOutput`, server-only). `videos.output_url` now stores the **S3 object key** (`outputObjectKey` in `packages/shared/src/storage.ts`); worker uploads to S3; web `getVideos` + API `GET /:id` presign on read; delete hits S3. Removed Supabase `uploadFrom`/`getPublicUrl`/`OUTPUT_BUCKET`. Legacy full-URL rows pass through via `isAbsoluteUrl`. New env: `AWS_REGION`/`S3_OUTPUT_BUCKET`/`S3_PRESIGN_EXPIRES_SEC`. Both apps typecheck clean.
  - [x] **Queue → Redis on the box** (Upstash retired). `docker-compose.yml` runs `redis:7-alpine` (appendonly + volume); compose sets `REDIS_URL=redis://redis:6379`. `queue.ts` unchanged (already scheme-agnostic).
  - [x] **Compute → EC2 + docker-compose.** Same `apps/api/Dockerfile`; compose runs `redis` + `api` (port 4000) + `worker` from one image (`rotpitch-api:latest`).
  - [x] **Web AWS SDK wiring.** `apps/web` gains `@aws-sdk/client-s3` + `s3-request-presigner`; `next.config.mjs` marks them `serverComponentsExternalPackages`. Read-only `s3:GetObject` IAM keys on Vercel; EC2 uses an instance role.
  - [x] **Docs.** `DEPLOY.md` rewritten for the AWS topology (S3 bucket+CORS, IAM, EC2 + compose, env tables, smoke test); `.env.example` + `CLAUDE.md` updated.
  - [ ] **External (owner):** create S3 bucket + CORS + IAM (instance role + read-only web user); launch EC2 (Ubuntu 24.04, ≥ t3.large, instance role); `docker compose up -d --build`; set Vercel AWS env; cut `NEXT_PUBLIC_API_URL` over to the EC2 API; smoke-test end-to-end.
